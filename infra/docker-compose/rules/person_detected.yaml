# Rule: alert when any person is detected anywhere in the scene.
# This fires immediately with any camera — good for verifying the full
# ingestion → inference → rule-engine → incident pipeline.
rule:
  name: person_detected
  zone: site
  condition:
    object: person
  action:
    type: alert
    severity: low
  enabled: true
