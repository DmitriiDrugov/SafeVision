# Rule: alert when 3 or more people are present in the scene simultaneously.
# Useful for testing the min_count condition without PPE detection.
rule:
  name: crowding_site
  zone: site
  condition:
    object: person
    min_count: 3
  action:
    type: alert
    severity: medium
    channel: dashboard
  enabled: true
